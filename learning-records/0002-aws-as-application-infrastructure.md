# AWS As Application Infrastructure

The user correctly framed AWS as a place to move application responsibilities such as API hosting, storage, and execution from local development into managed cloud services. Future lessons can assume this mental model and focus on mapping each project responsibility to a specific AWS service rather than reintroducing AWS from scratch.
