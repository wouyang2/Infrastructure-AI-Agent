# AWS Through The Infrastructure Agent

The user wants to learn AWS as an application developer by applying it to the Infrastructure AI Agent. The first useful AWS concepts are S3 object storage, SQL metadata separation, presigned URLs, and IAM least-privilege permissions because they map directly to the project's uploaded inspection media flow.
